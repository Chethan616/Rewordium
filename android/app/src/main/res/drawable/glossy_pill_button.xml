<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- State when the button is pressed -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <!-- A slightly darker, more solid blue when pressed -->
            <solid android:color="#005AE0" />
            <!-- Fully rounded corners to create the pill shape -->
            <corners android:radius="100dp" />
        </shape>
    </item>

    <!-- Default, unpressed state -->
    <item>
        <layer-list>
            <!-- Layer 1: The main blue gradient background -->
            <item>
                <shape android:shape="rectangle">
                    <gradient
                        android:angle="270"
                        android:startColor="#00A2FF"
                        android:endColor="#0062FF" />
                    <corners android:radius="100dp" />
                </shape>
            </item>
            
            <!-- Layer 2: A subtle white highlight for the "glossy" effect at the top -->
            <item android:bottom="20dp" android:left="2dp" android:right="2dp" android:top="2dp">
                <shape android:shape="rectangle">
                    <gradient
                        android:angle="270"
                        android:startColor="#4DFFFFFF"
                        android:endColor="#00FFFFFF" />
                    <corners android:radius="100dp" />
                </shape>
            </item>
            
            <!-- Layer 3: A thin border for definition -->
            <item>
                <shape android:shape="rectangle">
                    <solid android:color="@android:color/transparent" />
                    <stroke android:width="1dp" android:color="#4DFFFFFF" />
                    <corners android:radius="100dp" />
                </shape>
            </item>
        </layer-list>
    </item>
</selector>