<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="FlorisAppTheme.Splash" parent="Theme.SplashScreen">
        <item name="postSplashScreenTheme">@style/FlorisAppTheme</item>
        <item name="windowSplashScreenBackground">@color/splash_dark_bg</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_empty</item>

        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>
    </style>

    <style name="FlorisAppTheme" parent="android:style/Theme.Material.NoActionBar">
        <item name="android:colorPrimary">@color/colorPrimary</item>
        <item name="android:colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:colorAccent">@color/colorAccent</item>

        <!-- IMPORTANT: the following 2 colors MUST be references and not inline colors, or older
             versions of Android will not set the window background and thus causing weird
             rendering glitches!! -->
        <item name="android:windowBackground">@color/florisApp_windowBackground</item>
        <item name="android:colorBackground">@color/florisApp_windowBackground</item>

        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>
    </style>

    <style name="CrashDialogTheme" parent="android:style/Theme.Material.NoActionBar"/>

    <style name="FlorisImeTheme" parent="android:Theme.Material.InputMethod">
        <!-- Need this to prevent Xiaomi devices from messing with our beautiful UI -->
        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>
        <!-- FFS -->
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackground">@android:color/transparent</item>
    </style>

    <style name="FlorisAppTheme.Transparent" parent="android:style/Theme.Material.NoActionBar">
        <item name="android:colorPrimary">@android:color/transparent</item>
        <item name="android:colorPrimaryDark">@android:color/transparent</item>
        <item name="android:colorAccent">@android:color/transparent</item>

        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <!-- Premium dark theme for Android-Image-Cropper CropImageActivity.
         Must keep ActionBar so the Crop / confirm toolbar button is visible.
         colorPrimary = toolbar bg, colorPrimaryDark = status bar,
         textColorPrimary = toolbar title & menu text. -->
    <style name="CropperDarkTheme" parent="Theme.AppCompat">
        <item name="colorPrimary">@color/cropper_toolbar</item>
        <item name="colorPrimaryDark">@color/cropper_status_bar</item>
        <item name="colorAccent">@color/cropper_accent</item>
        <item name="android:textColorPrimary">@color/cropper_toolbar_text</item>
        <item name="android:textColorSecondary">@color/cropper_toolbar_text</item>
        <item name="android:windowBackground">@color/cropper_background</item>
        <item name="android:colorBackground">@color/cropper_background</item>
        <item name="android:navigationBarColor">@color/cropper_status_bar</item>
        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>
    </style>

</resources>
